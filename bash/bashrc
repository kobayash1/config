#
# ~/.bashrc
#

# If not running interactively, don't do anything
[[ $- != *i* ]] && return

alias ls='ls --color=auto'
PS1='[\u@\h \W]\$ '
PATH=$PATH:~/.gem/ruby/2.1.0/bin/:~/Scripts/
alias p='mpc toggle'
alias n='mpc next'
alias go='sudo systemctl start sddm'
