put this file in ~/.config/openbox to add Ctrl+Alt+t shortcut to open lxterminal and PrntScr 
screenshot hotkeys (remember to install imagemagick, scrot, curl, and xclip

cp lxde-rc.xml ~/.config/openbox
